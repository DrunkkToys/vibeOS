import{type SavingsPayload}from"../api"
export default function SavingsCard({savings}:{savings:SavingsPayload|null}){if(!savings)return<div class="card"><h3>Savings</h3><p>Loading...</p></div>
const total=savings.lifetime.delegation_usd+savings.lifetime.cache_usd;const st=savings.current_session.delegation_usd+savings.current_session.cache_usd;const a=savings.trend==="up"?"↑":savings.trend==="down"?"↓":"→";const tc=savings.trend==="up"?"trend-up":savings.trend==="down"?"trend-down":"trend-flat"
return(<div class="card"><h3>Savings <span class={`trend ${tc}`}>{a}</span></h3><table class="kv-table"><tbody><tr><td>Lifetime</td><td><strong>${total.toFixed(4)}</strong></td></tr><tr><td>Session</td><td><strong>${st.toFixed(4)}</strong></td></tr><tr><td>Cache Missed</td><td>${savings.lifetime.missed_context7_usd.toFixed(4)}</td></tr><tr><td>Rate / hr</td><td>${savings.savings_rate_per_hour.toFixed(4)}</td></tr><tr><td>Cache Hits</td><td>{savings.cache_hits_this_session}</td></tr><tr><td>Total Warns</td><td>{savings.lifetime.total_warns}</td></tr></tbody></table>{savings.current_session.tool_breakdown&&(<div class="breakdown"><h4>Tool Breakdown</h4><div class="bar-chart">{Object.entries(savings.current_session.tool_breakdown).map(([t,u])=><div class="bar-row"><span class="bar-label">{t}</span><div class="bar-track"><div class="bar-fill" style={`width:${Math.min(u/Math.max(...Object.values(savings.current_session.tool_breakdown))*100,100)}%`}></div></div><span class="bar-value">${u.toFixed(4)}</span></div>)}</div></div>)}</div>)}
