import{createSignal,onCleanup}from"solid-js";import{openSSE,fetchStatus,fetchSavings,type StatusPayload,type SavingsPayload}from"./api";import StatusCard from"./components/Status";import SavingsCard from"./components/Savings";import SessionsPanel from"./components/Sessions";import StressGauge from"./components/StressGauge";import Controls from"./components/Controls";import ReportsPanel from"./components/Reports";import BlackboxPanel from"./components/Blackbox"
type Tab="status"|"controls"|"reports"|"blackbox"
export default function App(){const[s,setS]=createSignal<StatusPayload|null>(null);const[sv,setSv]=createSignal<SavingsPayload|null>(null);const[tab,setTab]=createSignal<Tab>("status");const[conn,setConn]=createSignal(false);const cl=openSSE(d=>{setS(d.status);setSv(d.savings);setConn(true)});fetchStatus().then(setS).catch(()=>{});fetchSavings().then(setSv).catch(()=>{});onCleanup(cl);const tabs:{key:Tab;label:string}[]=[{key:"status",label:"Status"},{key:"controls",label:"Controls"},{key:"reports",label:"Reports"},{key:"blackbox",label:"Blackbox"}]
return(<div class="app"><header class="header"><div class="header-title"><h1>vibeOS</h1><span class="version">{s()?.version??"..."}</span></div><div class="header-indicators"><span class={`indicator ${conn()?"connected":"disconnected"}`}>{conn()?"live":"connecting..."}</span>{s()?.enabled===false&&<span class="indicator disabled">disabled</span>}</div></header><nav class="nav-tabs">{tabs.map(t=><button class={`tab ${tab()===t.key?"active":""}`}onClick={()=>setTab(t.key)}>{t.label}</button>)}</nav><main class="content">{tab()==="status"&&<div class="grid-2col"><StatusCard status={s()}/><SavingsCard savings={sv()}/><StressGauge status={s()}/><SessionsPanel/></div>}{tab()==="controls"&&<Controls status={s()}onAction={()=>{fetchStatus().then(setS);fetchSavings().then(setSv)}}/>}{tab()==="reports"&&<ReportsPanel/>}{tab()==="blackbox"&&<BlackboxPanel/>}</main></div>)}
